{% extends 'base.html.twig' %}

{% block title %}Modication auteur{% endblock %}

{% block body %}
    <div class="container my-5">
        <a href="{{ path('admin') }}"><i class="bi bi-arrow-bar-left"></i> Retour</a>

        <h2>Modifier l'auteur</h2>

        {{ include('author/_form.html.twig', {'button_label': 'Modifier'}) }}

        {{ include('author/_delete_form.html.twig') }}
    </div>
{% endblock %}
